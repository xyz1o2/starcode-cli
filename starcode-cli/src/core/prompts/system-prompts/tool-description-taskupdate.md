<!--
name: 'Tool Description: TaskUpdate'
description: Update task status or notes
-->
Update task status or append notes.

**Use for**: marking progress, recording work notes.
**NOT for**: creating tasks (use `Todo`), viewing (use `task_get`).

**Params**: `task_id`, `status` (optional), `notes` (optional)